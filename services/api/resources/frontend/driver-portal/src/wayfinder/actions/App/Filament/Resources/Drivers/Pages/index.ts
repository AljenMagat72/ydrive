import OverviewDrivers from './OverviewDrivers'
import ListDrivers from './ListDrivers'
import ScheduleDrivers from './ScheduleDrivers'
import DriverCMS from './DriverCMS'
import DriverSettings from './DriverSettings'
import ViewDriver from './ViewDriver'

const Pages = {
    OverviewDrivers: Object.assign(OverviewDrivers, OverviewDrivers),
    ListDrivers: Object.assign(ListDrivers, ListDrivers),
    ScheduleDrivers: Object.assign(ScheduleDrivers, ScheduleDrivers),
    DriverCMS: Object.assign(DriverCMS, DriverCMS),
    DriverSettings: Object.assign(DriverSettings, DriverSettings),
    ViewDriver: Object.assign(ViewDriver, ViewDriver),
}

export default Pages
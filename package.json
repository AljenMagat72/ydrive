{
  "name": "y-drive",
  "private": true,
  "scripts": {
    "deploy": "git checkout main && git rebase dev && git push origin main --force-with-lease"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.8.1",
    "typescript": "5.9.2"
  },
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ]
}

{
  "name": "y-drive",
  "private": true,
  "scripts": {
    "deploy": "git checkout main && git rebase dev && git push origin main --force-with-lease"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.8.1",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.2.15",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ]
}
